{
  "name": "greek-alphabet-flashcards",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "npm run setup && vite build",
    "preview": "vite preview",
    "serve": "vite preview --port 5000",
    "generate-icons": "node scripts/generateIcons.js",
    "generate-favicon": "node scripts/generateFavicon.js",
    "setup": "npm run generate-icons && npm run generate-favicon",
    "start": "npm run build && npm run serve"
  },
  "keywords": [],
  "author": "Greek Flashcards App",
  "license": "MIT",
  "description": "Learn the Greek alphabet with interactive flashcards and pronunciation. Progressive Web App with offline support.",
  "dependencies": {
    "vite": "^6.3.5",
    "vite-plugin-pwa": "^1.0.0"
  },
  "type": "module"
}
